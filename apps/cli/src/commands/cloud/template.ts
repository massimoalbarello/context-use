import { defineCommand } from "@parshjs/core";

export const command = defineCommand("cloud template", {
  description: "Inspect or apply default knowledge directories, guides, and managed pages.",
  options: {},
});
