node_modules/
dist/
.env
.env.*
!.env.example
.DS_Store
*.log
coverage/
# Local evaluation runs (the root path is retained for pre-migration artifacts).
.eval-results/
/eval/results/
/.eval-data/
# Knowledge bases exported from a run. Derived from datasets that are deliberately not
# vendored — LoCoMo is CC BY-NC — so the derivatives stay out of history too.
/eval/exports/
# A local choice of harness, model or eval, which never belongs in a commit.
/eval/config.local.json
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
.context-use/
/data/
apps/web/src/routeTree.gen.ts

# Local agent settings
.claude/settings.local.json
